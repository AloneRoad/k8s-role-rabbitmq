kind: PersistentVolume
apiVersion: v1
metadata:
  name: rabbitmq-pv2
  labels:
    type: local
    app: rabbitmq
spec:
  capacity:
    storage: 100Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/tmp/rabbitmq-pv2"